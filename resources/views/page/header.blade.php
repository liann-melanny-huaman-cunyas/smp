<header id="inicio">
    <!-- Barra superior -->
    @include('page.barrasuperior')

    <!-- Navegación principal -->
    @include('page.navbar')

    <!-- Slogan -->
    @include('page.slogan')
</header>
